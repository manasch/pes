#include <bits/stdc++.h>
// #define cal(op, x) \
// do {
//     static_assert(op == '*' || op == '+', "Invalid Operation"); \
//     ((op == '*') ? ((op) * (op) : ((op) + (op))))
// } while (0);
using namespace std;

int main() {
    // cout << cal(*, 10) << endl;
    return 0;
}

; WRITE A ALP TO FIND SUM OF N NATURAL NUMBERS
.DATA
	SUM: .WORD 0

.TEXT

	LDR R1, =SUM
	MOV R5, #0 ; INTERMEDIATE SUM
	MOV R4, #0 ; COUNT REGISTER
	LOOP:
		ADD R4, R4, #1 ; UPDATE COUNT
		ADD R5, R5, R4
		CMP R4, #10
		BNE LOOP
	STR R5, [R1]
	SWI 0x011

.END
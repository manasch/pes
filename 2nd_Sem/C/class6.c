#include<stdio.h>
#include<limits.h>

int main()
{
    /*int a = 10;
    int *p; //pointer, stores the address, right now not holding any value
    p=&a; //& is the address operator, therefore p will have the address of a
    printf("a is stored at %p and the value of a is %d",p,a);
    printf("\nvalue of a = %d",*p);*/

    /*int x = 10;
    int a = 10;
    int b = a++;
    int c = ++a;
    printf("%d %d %d",a,b,c); //side effect
    printf("\n%d", ++x);*/

    //printf("%d %d", INT_MAX, INT_MIN);

    printf("%d %d %f %f", 2/4, 2.0/4, 2/4.0, 2.0/4.0);
}
#include<stdio.h>

/*
int checkprime(int);
int main()
{
    int a;
    printf("Enter a number: ");
    scanf("%d",&a);
    if (checkprime(a))
        printf("%d is a prime number",a);
    else
        printf("%d is not a prime number",a);
}

int checkprime(int num)
{
    int i=2;
    while(i<=num/2)
    {
        if (num&i==0)
            return 0;
        else
            i++;
    }
    return 1;
}*/

/*
int decitooctal(int num)
{
    int oct =0;
    int temp=1;

    while (num!=0)
    {
        oct+=(num%8)*temp;
        num/=8;
        temp*=10;
    }

}
*/


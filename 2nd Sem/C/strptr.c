#include<stdio.h>

int main(){
    /*
    char x[] = "pes"; //stored in stack memory
    puts(x);
    x[0] = 'P';
    puts(x);*/
    /*
    char *p = "pes"; //p -- stack memory; "pes" is stored in the code area
    puts(p); // hence this is read only
    p[0] = 'P'; // write operation on data stored in code area
    puts(p);
    */
}